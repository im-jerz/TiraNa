# Copy this to .env and fill in real values.
# .env should be in .gitignore — never commit real secrets.

FLASK_ENV=development
SECRET_KEY=replace-with-a-long-random-string
JWT_SECRET_KEY=replace-with-another-long-random-string

# Oracle connection (python-oracledb, thin mode — no Oracle Client install needed)
# Format: oracle+oracledb://USERNAME:PASSWORD@HOST:PORT/?service_name=SERVICENAME
DATABASE_URL=oracle+oracledb://AIRBNB_HOST:Carlangelo%2319@localhost:1521/?service_name=FREEPDB1

MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=angelonovero69@gmail.com
MAIL_PASSWORD=chyrzyjglofvmrej
MAIL_DEFAULT_SENDER=angelonovero69@gmail.com

# Cloud storage (wired up later in upload_service.py)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Client backend API URL — for revenue queries via HTTP (never direct DB)
CLIENT_API_URL=http://localhost:5000